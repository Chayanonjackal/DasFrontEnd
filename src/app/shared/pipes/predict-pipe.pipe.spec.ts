import { PredictPipePipe } from './predict-pipe.pipe';

describe('PredictPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PredictPipePipe();
    expect(pipe).toBeTruthy();
  });
});
